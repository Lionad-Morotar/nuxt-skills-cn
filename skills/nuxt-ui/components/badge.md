# 徽章

一段简短的文字，用于表示状态或类别。

## 主要属性

- `label`：用于设置徽章的标签。
- `color`：用于更改徽章的颜色。
- `variant`：
- `size`：用于更改徽章的尺寸。
- `icon`：用于在徽章内显示 [Icon](/docs/components/icon)。
- `avatar`：用于在徽章内显示 [Avatar](/docs/components/avatar)。
- `class`：用于覆盖徽章的基础样式。

## 用法

```vue
<UBadge
  <!-- 这里放置属性 -->
/>
```
