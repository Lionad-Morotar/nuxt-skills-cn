# EditorDragHandle

编辑器中用于重新排序和选择模块的可拖拽句柄。

## 主要属性

- `icon`：用于自定义拖拽句柄图标。
- `options`：用于通过 [Floating UI 选项](https://floating-ui.com/docs/overview) 自定义定位行为。

## 使用方法

```vue
<UEditorDragHandle
  <!-- 属性在此处 -->
/>
```
