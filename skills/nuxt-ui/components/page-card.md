# PageCard

一种预设样式的卡片组件，用于显示标题、描述和可选链接。

## 主要属性

- `title`：用于设置卡片的标题。
- `description`：用于设置卡片的描述。
- `icon`：用于设置卡片的图标。
- `variant`：用于更改卡片的样式。
- `orientation`：用于更改默认插槽的方向。
- `reverse`：用于反转默认插槽的方向。

## 使用方法

```vue
<UPageCard
  <!-- 属性在此处 -->
/>
```
