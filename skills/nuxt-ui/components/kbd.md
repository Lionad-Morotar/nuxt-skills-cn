# Kbd

用于显示键盘按键的 kbd 元素。

## 关键属性

- `value`：用于设置 Kbd 的值。
- `color`：用于更改 Kbd 的颜色。
- `variant`：用于更改 Kbd 的变体。
- `size`：用于更改 Kbd 的尺寸。
- `class`：用于覆盖 Badge 的基础样式。

## 用法

```vue
<UKbd
  <!-- 这里放置属性 -->
/>
```
